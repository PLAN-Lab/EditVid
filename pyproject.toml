[build-system]
requires = ["setuptools==80.10.2", "wheel==0.46.3"]
build-backend = "setuptools.build_meta"

[project]
name = "editvid"
version = "0.1.0"
description = "EDITVID training-free video-editing pipeline"
requires-python = "==3.11.*"

[tool.setuptools]
packages = ["editvid"]
